{
  "name": "frisk",
  "version": "1.0.0",
  "description": "Generate Pocket Status",
  "main": "index.js",
  "scripts": {
    "status": "rm -rf dist/ && tsc && node dist/status.js",
    "sweep": "rm -rf dist/ && tsc && node dist/sweep.js",
    "build": "rm -rf dist/ && tsc"
  },
  "repository": "https://github.com/nymd/frisk",
  "license": "MIT",
  "dependencies": {
    "@influxdata/influxdb-client": "^1.20.0",
    "@types/request-promise": "^4.1.47",
    "child_process": "^1.0.2",
    "cron": "^1.8.2",
    "csv-parser": "^3.0.0",
    "minimist": "^1.2.5",
    "request": "^2.88.2",
    "request-promise": "^4.2.6",
    "typescript": "^4.2.2",
    "url": "^0.11.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.31"
  }
}

{
  "name": "frisk",
  "version": "1.0.0",
  "description": "Generate Pocket Status",
  "main": "dist/index.js",
  "scripts": {
    "sweep": "rm -rf dist/ && tsc && node dist/sweep.js",
    "server": "rm -rf dist/ && tsc && node dist/index.js",
    "build": "rm -rf dist/ && tsc"
  },
  "repository": "https://github.com/nymd/frisk",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "child_process": "^1.0.2",
    "csv-parser": "^3.0.0",
    "dotenv": "^8.2.0",
    "prom-client": "^13.1.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.31"
  }
}
